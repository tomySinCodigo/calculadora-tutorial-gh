name: "ejemplo de usando una variable de repositorio"

on: [workflow_dispatch]

jobs:
  say-hello:
    runs-on: ubuntu-latest

    env:
      My_VAR: mi variable de entorno

    steps:
    - name: test variable de repo
      run: echo "el valor es ${{ vars.REPO_MI_VAR_1 }}"